# Codex-Gemini Bridge Configuration

PORT=3000
NODE_ENV=development

# Providers
# Options: mock, gemini, ollama
DEFAULT_PROVIDER=mock

# Ollama Cloud (Optional)
OLLAMA_CLOUD_URL=
OLLAMA_CLOUD_MODEL=

# Gemini CLI Path (Optional, if not in PATH)
GEMINI_CLI_PATH=gemini
